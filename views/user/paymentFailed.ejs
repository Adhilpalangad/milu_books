<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        body {
            background-color: #f7f7f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }
        .fail-container {
            text-align: center;
        }
        .crossmark {
            width: 150px;
            height: 150px;
            border: 10px solid #dc3545; /* Red border */
            border-radius: 50%;
            position: relative;
            display: inline-block;
            background: white;
            margin-bottom: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            overflow: hidden;
        }
        .crossmark svg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }
        .crossmark circle {
            fill: none;
            stroke: #dc3545;
            stroke-width: 10;
            stroke-dasharray: 440;
            stroke-dashoffset: 440;
            animation: drawCircle 0.8s ease forwards;
        }
        .crossmark path {
            fill: none;
            stroke: #dc3545;
            stroke-width: 10;
            stroke-linecap: round;
            stroke-dasharray: 100;
            stroke-dashoffset: 100;
            animation: drawCross 0.5s ease forwards;
            animation-delay: 0.7s;
        }
        @keyframes drawCircle {
            to {
                stroke-dashoffset: 0;
            }
        }
        @keyframes drawCross {
            to {
                stroke-dashoffset: 0;
            }
        }
    </style>
</head>
<body>
    <div class="fail-container">
        <div class="crossmark animate_animated animate_zoomIn">
            <svg viewBox="0 0 100 100">
                <circle cx="50" cy="50" r="40"></circle>
                <path d="M35 35 L65 65 M65 35 L35 65"></path> <!-- Cross -->
            </svg>
        </div>
        <div class="alert alert-danger">
            <h2>Payment Failed!</h2>
            <p>Unfortunately, your payment could not be processed. Please try again.</p>
            <p>If the issue persists, contact our support team.</p>
            <a href="/profile/orders" class="btn btn-primary">Try Again</a>
            <a href="/contact" class="btn btn-secondary">Contact Support</a>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
